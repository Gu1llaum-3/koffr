# Build output
/bin/
/dist/
# Anchored: a bare 'koffr' would also exclude the cmd/koffr/ directory.
/koffr
*.test
*.out

# Probe artifacts and probe key material. The rig regenerates all of it; none
# of it belongs in a public repository, least of all the private keys.
/spikes/out/
/spikes/tls/
/spikes/ssh/
/spikes/bastion/authorized_keys
/spikes/**/*.tar
/spikes/**/*.zst
/spikes/**/*.age
/spikes/**/*.key
/spikes/**/*.crt
/spikes/**/*.jsonl

# Local working documents (French), deliberately kept out of the repository
/cahier-des-charges.md
/architecture.md
/roadmap.md
/plan.md

# Editor / OS
.DS_Store
.idea/
.vscode/
