// Package protocol carries the messages koffr and koffr-server exchange, and the rules that validate them. It is public and imports nothing else from this repository, so that the server can depend on it without depending on the agent (ADR-0001).
package protocol
